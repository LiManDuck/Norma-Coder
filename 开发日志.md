# 参考代码

本项目是以python来实现类claude code的终端编程助手的能力，具体的 你可以参考/mnt/e/code/claude-code 的代码


# 工作约束

- 每当你完成某个开发任务时，在这里标记为已完成
- ai-work 文件夹作为你的文件夹, 你可以在其中记录任何你觉得对当前代名仓有利的信息 你将长期参与该代码仓的开发需求 合理利用该文件夹有助于你的开发进展和规划
- 每一个开发需求 其都需要基于广泛的分析实现，选择优秀 符合当前当前代码仓架构的实现方式 而不仅仅作为一个通过测试的任务
- 我仍然建议你对于参考claude code 的代码实现方案，并作为文档不断汇总记录到ai-work下 ; 同时将当前代码仓的架构设计 核心写在其中，并在ai-work下维护一个入口的目录文档
- 当你不知道一个模块该如何实现的时候，可以去参考claude code的设计


# 2026年6月18日 开发任务
- 1. 修正prompt文件夹 改为test文件夹
- 2. tasks 文件夹 应该是一个工具吧 应该要移动到 tool下吧
- 3. agent的呢 核心的agent的在哪, 你需要review 当前的代码仓的整体实现, 找出所有的问题 , 写在ai-work目录下 当前代码仓有重大问题, 各个模块 完全是相互独立的


# 2026年6月15日 开发任务2
- 1. 确认mcp服务器可以在配置文件的mcp字段进行配置，从而可以实现启动cli的时候加载
- 2. 确认下面的所有开发的内容，在sdk侧都可以进行配置，例如我导入这个包，定义agent执行的时候，可以自行配置相关的子agent，工具权限等信息
- 3. 新增执行模式的前端的切换和现实，组合键 shift+tab可以实现执行模式，plan , edit  , auto的来回切换
- 4. 新增一个session概念，即用户启动cli，或启动一个sdk时候，当前会话会是一个session-id， 相同的session-id下，用户每次输入，即触发agent的执行循环，如果在cli段，用户再次输入的话，则还是再同一个session-id里，每个sessionid的信息都要保存在本地的路径，默认为用户目录下的配置文件的文件夹下，支持当用户/resume的时候列出若干历史会话  ，这一点和claude-code的实现非常相似，你需要去阅读claude-code的代码来实现






# 2026年6月15日 开发任务 （已完成）

- 1. ✅ 工具目录从 prompt/tool/ 迁移到 tool/，更新所有导入
- 2. ✅ 实现 compaction 系统：当上下文超过阈值（默认75%）自动触发压缩，支持 token 估算
- 3. ✅ 实现 CLI command 系统：命令注册机制，默认命令 /new, /help, /exit, /clear, /model, /compact, /status
- 4. ✅ 重建消息和事件系统：以 finish_reason 驱动主循环（stop→结束，tool_calls→继续），支持一次回复同时包含文本和 tool_calls
- 5. ✅ 支持 MCP 工具：实现 MCPClient（stdio 传输，JSON-RPC 2.0）、MCPTool 包装器、MCPManager 管理器
- 6. ✅ 支持多 Provider 模型配置：providers 字段配置多个模型提供商，/model 命令列出和切换模型

## 额外完成的关键修复
- ✅ 修复 OpenAILLM 类缺失（原文件被错误写入了 RepoMemory 代码）
- ✅ 修复 EditTool.is_file_read 始终返回 True 的安全漏洞
- ✅ 补全所有缺失的 __init__.py 文件










# 2026年6月14日 （ 已完成）

工具侧实现： 
- 新增subagent 机制，即允许使用Agent工具，输入参数为 prompt, session_id （可选），run_background （默认为false) 三个参数

- 实现一个hook系统，工具权限系统，这两个系统都基于系统总线messagebus来完成，具体的实现可以先简易一点，例如工具权限先设置一个权限模式，三个模式，plan , edit, auto ,  同时允许用户在配置文件的permission 配置各个工具的权限； 对于hook系统，也可以先实现的简单一点，就是允许用户在配置文件的hook 字段，开放一些特定的事件，例如 toolexecuteafter，session-begin , 等，允许配置一个command直接执行

# 2026年6月14日 （ 开发任务2 已完成）
- 新增一个skill 系统，同时也是一个skill工具 ，其会从当前的配置目录下的skills目录寻找skill文件，支持skill工具
- 将todowrite工具改为你现在的（claude code) 的Task工具（可能包含多个task工具） 
- 新增一个reminder系统，其实现一个可以注册reminder的类，同时reminder系统在实际的agent loop 中会触发，并在工具的结果后或者在用户输入后，追加到上下文中，其所有的reminder文本，都默认采用 <system-reminder> </system-reminder>标签包裹


